<view class="page">
  <view class="card">
    <view class="title">SAGPT</view>
    <view class="muted">登录后填写企业出海咨询登记</view>
    <view wx:if="{{error}}" class="error">{{error}}</view>
    <button class="primary" loading="{{loading}}" bindtap="login">微信登录</button>
  </view>
</view>
